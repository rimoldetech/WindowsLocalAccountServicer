# WindowsLocalAccountManager
A robust PowerShell script for managing local Windows user accounts. Designed for use with TacticalRMM and similar RMM platforms, but equally useful interactively — run it without arguments to get a full text-based menu interface.
